spring:
  datasource:
    url: jdbc:mysql://localhost:3307/sfg_prod
    username: sfg_prod_user
    password: T=6He6X?n?(#Y8$
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
    database: mysql
    show-sql: false
    defer-datasource-initialization: true
  sql:
    init:
      platform: mysql